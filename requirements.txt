# TUDOR reproducibility — pinned Python deps (Python 3.12)
# Install:  python -m pip install -r requirements.txt
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
statsmodels==0.14.2
lifelines==0.29.0
python-docx==1.1.2
matplotlib==3.9.0
pyarrow==16.1.0
# Figures are rendered in R (see scripts/*.R) — not required for the Python pipeline.
